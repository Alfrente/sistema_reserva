server.port=${PORT}
spring.datasource.url=${BASE-DATOS}
spring.datasource.username=${USERNAME}
spring.datasource.password=${PASSWORD}
spring.output.ansi.enabled=ALWAYS
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

